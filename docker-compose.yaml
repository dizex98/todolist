version: '3.3'
services:
  frontend:
    build: 
      dockerfile: Dockerfile_nginx
      context: .
    ports:
      - "80:80"
    depends_on:
      - backend
      - mongodb-0.mongodb-headless
    networks:
      - portfolio_net
  mongodb-0.mongodb-headless:
    build: 
      dockerfile: Dockerfile_mongo
      context: .
    restart: always
    networks:
      - portfolio_net
  backend:
    build: .
    depends_on:
      - mongodb-0.mongodb-headless
    networks:
      - portfolio_net
volumes:
  mongo_data:
networks:
  portfolio_net:
    external: true
